<%= form_with(model: doubt, local: true) do |form| %>
  <% if doubt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doubt.errors.count, "error") %> prohibited this doubt from being saved:</h2>

      <ul>
      <% doubt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :doubt_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :doubt_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
